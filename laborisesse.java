public String processItem(String item, String s) {
    if (item == null) {
        return s;
    }
    // Other processing logic if item is not null
    return item.toUpperCase();  // Example processing, returning item in uppercase
}
